# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Local artifacts — never publish
*.zip
openspec-cursor-toolkit.zip

# AI session output (per-project runtime; not part of the toolkit)
.cursor/context/
.cursor/QAtest/
.cursor/postman/
.cursor/tmp/
.claude/context/
.claude/QAtest/
.claude/postman/
.claude/tmp/

# Export-spec registry (machine-local project list)
**/.cursor/context/export-spec/
**/.cursor-local/
**/.claude/context/export-spec/
**/.claude-local/

# The toolkit's own OpenSpec changes ARE versioned on purpose (its features are specified with OpenSpec).
# Uncomment only in a fork that keeps OpenSpec changes exclusively in app repos.
# openspec/changes/
